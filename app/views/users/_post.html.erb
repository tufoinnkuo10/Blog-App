<li class="user_posts">
  <%= link_to post.title, user_post_path(id: post.id) %>
  <br>
  <p><%= post.text %></p>
  <h3>
    comments: <%= post.commentscounter == nil ? 0 : post.commentscounter %>,
    Likes: <%= post.likescounter == nil ? 0 : post.likes.counter %>
  </h3>
</li>
