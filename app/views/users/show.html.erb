<div class="user_show">
  <%= render @user %>
  <h2  class="bio"><span>Bio:</span><br/>
    <%= @user.bio %>
  </h2>
  <%= render 'post', posts: @user.posts %>
  <div class="all_post">
    <button type="button" class="btn btn-secondary"><%= link_to "existing user post", user_posts_path(@user.id) %></button>
  </div>
</div>
